@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script type="text/javascript">

    LayoutApp.controller('MyController', function ($scope, $http, $timeout) {

          $(document).ready(function () {

            var queryString = new Array();
            if (window.location.search.split('?').length > 1) {
                var params = window.location.search.split('?')[1].split('&');
                for (var i = 0; i < params.length; i++) {
                    var key = params[i].split('=')[0];
                    var value = decodeURIComponent(params[i].split('=')[1]);
                    queryString[key] = value;
                }
            }
              if (queryString["BkgPassID"] != null) {

                  $scope.RegId = queryString["BkgPassID"].toString();
                  $scope.HDBLId = queryString["BOLRegID"].toString();
                  $scope.FillExistingBkgNumber(queryString["BkgPassID"].toString());
                  $scope.FillExistingFreightTraiffCharges();
                  $scope.FillExistingTerminalTraiffCharges();
                  $scope.FillExistingHalageTraiffCharges();
                  $scope.FillExistingLocalTraiffCharges();

              }
        });

        $scope.DynamicGridFreight = [{ RCID: "", CntrTypes: "", ChargeCode: "", ChargeCodeID: "", Currency: "", CurrencyID: "", PaymentMode:"", PaymentModeID: "", ReqRate: "", ManifRate: "", CustomerRate: "", RateDiff: "", Qty: "", BillAmt: "", InvoiceNo: "", InvID:""}];
        $scope.DynamicGridHaulage = [{ RCID: "", CntrTypes: "", ChargeCode: "", ChargeCodeID: "", Currency: "", CurrencyID: "", PaymentMode: "", PaymentModeID: "", ReqRate: "", ManifRate: "", CustomerRate: "", RateDiff: "", Qty: "", BillAmt: "", InvoiceNo: "", InvID: "" }];
        $scope.DynamicGridTermainal = [{ RCID: "", CntrTypes: "", ChargeCode: "", ChargeCodeID: "", Currency: "", CurrencyID: "", PaymentMode: "", PaymentModeID: "", ReqRate: "", ManifRate: "", CustomerRate: "", RateDiff: "", Qty: "", BillAmt: "", InvoiceNo: "", InvID: "" }];
        $scope.DynamicGridLocalCharges = [{ RCID: "", CntrTypes: "", ChargeCode: "", ChargeCodeID: "", Currency: "", CurrencyID: "", PaymentMode: "", PaymentModeID: "", ReqRate: "", ManifRate: "", CustomerRate: "", RateDiff: "", Qty: "", BillAmt: "", InvoiceNo: "", InvID: "" }];

        $scope.FillExistingFreightTraiffCharges = function () {
            var Datavalue = {};

            Datavalue.TraiffRegular = 135;
            Datavalue.BkgID = $scope.RegId;
            Datavalue.BLID = $scope.HDBLId;
            Datavalue.PaymentModeID = 18;
            $http({
                method: 'POST',
                url: '/api/ExpTraffView/ExpTariffView',
                data: JSON.stringify(Datavalue),
            }).then(function (result) {
                $timeout(function () {

                    if (result.data.length > 0) {

                        if (result.data[0].ChargeCodeID != "") {
                            $scope.DynamicGridFreight.splice(0, 1);
                        }

                        angular.forEach(result.data, function (value, key) {

                            $scope.DynamicGridFreight.push({
                                'RCID': result.data[key].RCID,
                                'CntrTypes': result.data[key].CntrTypes,
                                'ChargeCode': result.data[key].ChargeCode,
                                'Currency': result.data[key].Currency,
                                'PaymentMode': result.data[key].PaymentMode,
                                'ReqRate': result.data[key].ReqRate,
                                'ManifRate': result.data[key].ManifRate,
                                
                                'RateDiff': result.data[key].RateDiff,
                                'Qty': result.data[key].Qty,
                                'CustomerRate': result.data[key].Amt,
                                'ManifRate': result.data[key].ManifRate,
                     
                                'BillAmt': result.data[key].BillAmt,
                                'InvoiceNo': result.data[key].InvoiceNo,
                                'InvID': result.data[key].InvID,

                            });

                        });
                    }
                });
            }, function (result) {

                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            });
        }

        $scope.FillExistingTerminalTraiffCharges = function () {
            var Datavalue = {};

            Datavalue.TraiffRegular = 136;
            Datavalue.BkgID = $scope.RegId;
            Datavalue.BLID = $scope.HDBLId;
            Datavalue.PaymentModeID = 18;
            $http({
                method: 'POST',
                url: '/api/ExpTraffView/ExpTariffView',
                data: JSON.stringify(Datavalue),
            }).then(function (result) {
                $timeout(function () {

                    if (result.data.length > 0) {

                        if (result.data[0].ChargeCodeID != "") {
                            $scope.DynamicGridTermainal.splice(0, 1);
                        }

                        angular.forEach(result.data, function (value, key) {

                            $scope.DynamicGridTermainal.push({
                                'RCID': result.data[key].RCID,
                                'CntrTypes': result.data[key].CntrTypes,
                                'ChargeCode': result.data[key].ChargeCode,
                                'Currency': result.data[key].Currency,
                                'PaymentMode': result.data[key].PaymentMode,
                                'ReqRate': result.data[key].ReqRate,
                                'ManifRate': result.data[key].ManifRate,

                                'RateDiff': result.data[key].RateDiff,
                                'Qty': result.data[key].Qty,
                                'CustomerRate': result.data[key].Amt,
                                'ManifRate': result.data[key].ManifRate,

                                'BillAmt': result.data[key].BillAmt,
                                'InvoiceNo': result.data[key].InvoiceNo,
                                'InvID': result.data[key].InvID

                            });

                        });
                    }
                });
            }, function (result) {

                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            });
        }


        $scope.FillExistingHalageTraiffCharges = function () {
            var Datavalue = {};

            Datavalue.TraiffRegular = 137;
            Datavalue.BkgID = $scope.RegId;
            Datavalue.BLID = $scope.HDBLId;
            Datavalue.PaymentModeID = 18;
            $http({
                method: 'POST',
                url: '/api/ExpTraffView/ExpTariffView',
                data: JSON.stringify(Datavalue),
            }).then(function (result) {
                $timeout(function () {

                    if (result.data.length > 0) {

                        if (result.data[0].ChargeCodeID != "") {
                            $scope.DynamicGridHaulage.splice(0, 1);
                        }

                        angular.forEach(result.data, function (value, key) {

                            $scope.DynamicGridHaulage.push({
                                'RCID': result.data[key].RCID,
                                'CntrTypes': result.data[key].CntrTypes,
                                'ChargeCode': result.data[key].ChargeCode,
                                'Currency': result.data[key].Currency,
                                'PaymentMode': result.data[key].PaymentMode,
                                'ReqRate': result.data[key].ReqRate,
                                'ManifRate': result.data[key].ManifRate,

                                'RateDiff': result.data[key].RateDiff,
                                'Qty': result.data[key].Qty,
                                'CustomerRate': result.data[key].Amt,
                                'ManifRate': result.data[key].ManifRate,

                                'BillAmt': result.data[key].BillAmt,
                                'InvoiceNo': result.data[key].InvoiceNo,
                                'InvID': result.data[key].InvID,

                            });

                        });
                    }
                });
            }, function (result) {

                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            });
        }

        $scope.FillExistingLocalTraiffCharges = function () {
            var Datavalue = {};

            Datavalue.TraiffRegular = 138;
            Datavalue.BkgID = $scope.RegId;
            Datavalue.BLID = $scope.HDBLId;
            Datavalue.PaymentModeID = 18;
            $http({
                method: 'POST',
                url: '/api/ExpTraffView/ExpTariffView',
                data: JSON.stringify(Datavalue),
            }).then(function (result) {
                $timeout(function () {

                    if (result.data.length > 0) {

                        if (result.data[0].ChargeCodeID != "") {
                            $scope.DynamicGridLocalCharges.splice(0, 1);
                        }

                        angular.forEach(result.data, function (value, key) {

                            $scope.DynamicGridLocalCharges.push({
                                'RCID': result.data[key].RCID,
                                'CntrTypes': result.data[key].CntrTypes,
                                'ChargeCode': result.data[key].ChargeCode,
                                'Currency': result.data[key].Currency,
                                'PaymentMode': result.data[key].PaymentMode,
                                'ReqRate': result.data[key].ReqRate,
                                'ManifRate': result.data[key].ManifRate,

                                'RateDiff': result.data[key].RateDiff,
                                'Qty': result.data[key].Qty,
                                'CustomerRate': result.data[key].Amt,
                                'ManifRate': result.data[key].ManifRate,

                                'BillAmt': result.data[key].BillAmt,
                                'InvoiceNo': result.data[key].InvoiceNo,
                                'InvID': result.data[key].InvID,

                            });

                        });
                    }
                });
            }, function (result) {

                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            });
        }



        $scope.FillExistingBkgNumber = function (BkgID) {
            var Datavalue = {};
            Datavalue.ID = BkgID;
            $http({
                method: 'POST',
                url: '/api/DocumentApi/BookingExistingViewRecord',
                data: JSON.stringify(Datavalue),
            }).then(function (result) {
                $timeout(function () {
                    if (result.data.length > 0) {

                        $scope.BindBkgNumber = result.data[0].BookingNo;
                    }
                });
            }, function (result) {

                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            });
        }

        $scope.getPaging = function (tab) {

            var myp = $scope.RegId;
            var myp1 = $scope.HDBLId;

            if (tab == 1) {
                if (myp != "0") {
                    var url = "/Document/Exptariffview?BkgPassID=" + encodeURIComponent(myp) + "&BOLRegID=" + encodeURIComponent(myp1);
                    window.location.href = url;
                }
                else {
                    ShowPopup("Please Create Shipment Details")
                    return false;
                }
            }
            else if (tab == 2) {
                if (myp != "0") {
                    var url = "/Document/ExptariffCast?BkgPassID=" + encodeURIComponent(myp) + "&BOLRegID=" + encodeURIComponent(myp1);
                    window.location.href = url;
                }
                else {
                    ShowPopup("Please Create Shipment Details")
                    return false;
                }
            }

            return false;
        }


        $scope.BackToBOLPage = function () {

            var url = "/Document/BOL?BOLRegID=" + encodeURIComponent($scope.HDBLId) + "&BkgPassID=" + encodeURIComponent($scope.RegId);
            window.location.href = url;
        }


        $scope.PDFFileOpen = function (InvID) {

            window.open('@Url.Action("InvoicePDF", "InvoicePDF")?idv=' + InvID);
        }

    });
    function ShowPopup(message) {
        swal(message);
    };
</script>
<style>
    .downbtn .btn {
        padding: 6px 12px;
    }

    .modal-dialog .modal-header .modal-title {
        margin-top: -13px !important;
        color: #fff !important;
        font-size: 18px;
    }

    .modal-dialog .modal-header {
        background-color: #1e4383 !important;
    }
</style>
<link href="~/assets/css/common.css" rel="stylesheet" />
<div ng-app="MyApp" ng-controller="MyController">
    <div id="overlay" style="display:none;">
        <div class="loadcontainer">
            <div class="spinner"></div>
            <p>Loading Please Wait</p>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12 backbtn" style="text-align:right;">
            <a href="#" ng-click="BackToBOLPage();"  type="submit" class="btn btn-fill btnback btn-success btn-round btntop">
                <i class="material-icons">reply</i> Back
            </a>
            @*<a href="#" type="submit" class="btn btn-fill btnedit btn-warning btn-round btntop">
                <i class="material-icons">edit</i>
            </a>*@
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="card">
              
                <div class="row">
                    <div class="col-md-6">
                        <nav>
                            <div class="nav nav-tabs nav-fill" id="nav-tab" role="tablist">
                                <a class="nav-item nav-link active" ng-click="getPaging(1);" id="nav-home-tab" data-toggle="tab" href="#nav-home" role="tab" aria-controls="nav-home" aria-selected="true">RATE DETAILS</a>
                                <a class="nav-item nav-link" ng-click="getPaging(2);" id="nav-Key-tab" data-toggle="tab" href="#nav-home" role="tab" aria-controls="nav-home" aria-selected="true">COST DETAILS</a>

                            </div>
                        </nav>
                    </div>
                    <div class="col-md-6">

                    </div>

                    </div>
                <div class="card-body">

                    <div class="row">
                        <div class="card-header card-header-primary card-header-text" style="width: 100%!important;">
                            <div class="card-text" style="background-color: #e6e6e6!important; color: #000!important;background:none!important; box-shadow: none!important; width: 100%!important; padding: 7px 0px; " ortant;">
                                <div class="row" style="background-color: #ededed; padding: 8px;">
                                    <div class="col-md-6">
                                        <h1 class="card-title" style="color: #000; font-weight: 500; font-size:18px;">Tariff Details</h1>
                                    </div>
                                    <div class="col-md-6" style="text-align:right;">
                                      <span style="color: red; font-size: 20px;">BLNo: <span style="color: red; font-size: 18px;" ng-bind="BindBkgNumber"></span> </span>
                                    </div>
                                </div>
                                   
                                </div>
                        </div>

                        <div class="col-md-12">
                            <hl>FREIGHT</hl>
                        </div>
                        <div class="col-md-12">
                            <div class="material-datatables">

                                <table class="table table-striped table-no-bordered table-hover" cellspacing="0" width="100%" style="width:100%">
                                    <thead>
                                        <tr>
                                            <th>SNo</th>

                                            <th>Equip Type</th>
                                            <th>Qty</th>
                                            <th>Charge Description</th>
                                            <th>Currency</th>
                                            <th>Payment Mode</th>

                                            <th>Manifest Rate</th>
                                            <th>Customer Rate</th>
                                            <th>Rate Different</th>
                                            <th>Billable Amt</th>
                                            <th>Invoice No</th>


                                        </tr>
                                    </thead>

                                    <tbody>
                                        <tr ng-repeat="gRow in DynamicGridFreight">
                                            <td>{{$index+1}}</td>
                                            <td>{{gRow.CntrTypes}}</td>
                                            <td>{{gRow.Qty}}</td>
                                            <td>{{gRow.ChargeCode}}</td>
                                            <td>{{gRow.Currency}}</td>
                                            <td>{{gRow.PaymentMode}}</td>

                                            <td class="amtrgt">{{gRow.ManifRate}}</td>
                                            <td class="amtrgt">{{gRow.CustomerRate}}</td>
                                            <td class="amtrgt">{{gRow.RateDiff}}</td>
                                            <td class="amtrgt">{{gRow.BillAmt}} </td>
                                            <td><a href="#" ng-click="PDFFileOpen(gRow.InvID)"> {{gRow.InvoiceNo}} </a></td>
                                        </tr>

                                    </tbody>
                                </table>

                            </div>
                        </div>
                        <div class="col-md-12">
                            <hl>TERMINAL HANDLING</hl>
                        </div>
                        <div class="col-md-12">
                            <div class="material-datatables">

                                <table class="table table-striped table-no-bordered table-hover" cellspacing="0" width="100%" style="width:100%">
                                    <thead>
                                        <tr>
                                            <th>SNo</th>

                                            <th>Equip Type</th>
                                            <th>Qty</th>
                                            <th>Charge Description</th>
                                            <th>Currency</th>
                                            <th>Payment Mode</th>

                                            <th>Manifest Rate</th>
                                            <th>Customer Rate</th>
                                            <th>Rate Different</th>
                                            <th>Billable Amt</th>
                                            <th>Invoice No</th>


                                        </tr>
                                    </thead>

                                    <tbody>
                                        <tr ng-repeat="gRow in DynamicGridTermainal">
                                            <td>{{$index+1}}</td>
                                            <td>{{gRow.CntrTypes}}</td>
                                            <td>{{gRow.Qty}}</td>
                                            <td>{{gRow.ChargeCode}}</td>
                                            <td>{{gRow.Currency}}</td>
                                            <td>{{gRow.PaymentMode}}</td>

                                            <td class="amtrgt">{{gRow.ManifRate}}</td>
                                            <td class="amtrgt">{{gRow.CustomerRate}}</td>
                                            <td class="amtrgt">{{gRow.RateDiff}}</td>
                                            <td class="amtrgt">{{gRow.BillAmt}} </td>
                                            <td><a href="#" ng-click="PDFFileOpen(gRow.InvID)"> {{gRow.InvoiceNo}} </a></td>
                                        </tr>

                                    </tbody>
                                </table>

                            </div>
                        </div>

                        <div class="col-md-12">
                            <hl>HAULAGE</hl>
                        </div>

                        <div class="col-md-12">
                            <div class="material-datatables">

                                <table class="table table-striped table-no-bordered table-hover" cellspacing="0" width="100%" style="width:100%">
                                    <thead>
                                        <tr>
                                            <th>SNo</th>

                                            <th>Equip Type</th>
                                            <th>Qty</th>
                                            <th>Charge Description</th>
                                            <th>Currency</th>
                                            <th>Payment Mode</th>

                                            <th>Manifest Rate</th>
                                            <th>Customer Rate</th>
                                            <th>Rate Different</th>
                                            <th>Billable Amt</th>
                                            <th>InvoiceNo</th>


                                        </tr>
                                    </thead>

                                    <tbody>
                                        <tr ng-repeat="gRow in DynamicGridHaulage">
                                            <td>{{$index+1}}</td>
                                            <td>{{gRow.CntrTypes}}</td>
                                            <td>{{gRow.Qty}}</td>
                                            <td>{{gRow.ChargeCode}}</td>
                                            <td>{{gRow.Currency}}</td>
                                            <td>{{gRow.PaymentMode}}</td>

                                            <td class="amtrgt">{{gRow.ManifRate}}</td>
                                            <td class="amtrgt">{{gRow.CustomerRate}}</td>
                                            <td class="amtrgt">{{gRow.RateDiff}}</td>
                                            <td class="amtrgt">{{gRow.BillAmt}} </td>
                                            <td><a href="#" ng-click="PDFFileOpen(gRow.InvID)"> {{gRow.InvoiceNo}} </a></td>

                                        </tr>

                                    </tbody>
                                </table>

                            </div>
                        </div>

                        <div class="col-md-12">
                            <hl>LOCAL CHARGES</hl>
                        </div>
                        <div class="col-md-12">
                            <div class="material-datatables">

                                <table class="table table-striped table-no-bordered table-hover" cellspacing="0" width="100%" style="width:100%">
                                    <thead>
                                        <tr>
                                            <th>SNo</th>

                                            <th>Equip Type</th>
                                            <th>Qty</th>
                                            <th>Charge Description</th>
                                            <th>Currency</th>
                                            <th>Payment Mode</th>

                                            <th>Manifest Rate</th>
                                            <th>Customer Rate</th>
                                            <th>Rate Different</th>
                                            <th>Billable Amt</th>
                                            <th>InvoiceNo</th>


                                        </tr>
                                    </thead>

                                    <tbody>
                                        <tr ng-repeat="gRow in DynamicGridLocalCharges">
                                            <td>{{$index+1}}</td>
                                            <td>{{gRow.CntrTypes}}</td>
                                            <td>{{gRow.Qty}}</td>
                                            <td>{{gRow.ChargeCode}}</td>
                                            <td>{{gRow.Currency}}</td>
                                            <td>{{gRow.PaymentMode}}</td>

                                            <td class="amtrgt">{{gRow.ManifRate}}</td>
                                            <td class="amtrgt">{{gRow.CustomerRate}}</td>
                                            <td class="amtrgt">{{gRow.RateDiff}}</td>
                                            <td class="amtrgt">{{gRow.BillAmt}} </td>
                                            <td><a href="#" ng-click="PDFFileOpen(gRow.InvID)"> {{gRow.InvoiceNo}} </a></td>

                                        </tr>

                                    </tbody>
                                </table>

                            </div>
                        </div>

                    </div>
                    <input type="hidden" ng-model="HDBLId" id="HDBLId" />
                    <input type="hidden" ng-model="RegId" id="RegId" />
                    @*<div class="row">
            <div class="col-md-12" style="text-align:right;">
                <div class="ml-auto">
                    <button type="submit" ng-click="RecordSave();" class="btn btn-fill btn-success">
                        <i class="material-icons">save</i>
                        Save
                    </button>

                </div>
            </div>
        </div>*@
                </div>
                </div>
        </div>
    </div>

    </div>


