@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/css/select2.min.css" rel="stylesheet">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/js/select2.full.min.js"></script>

<style>
    .btn-success.btnmove1 {
        width: 140px;
        padding: 5px 0;
        background-color: #0585b5;
        border-color: #0585b5;
    }

    .btn-success.btnmove2 {
        width: 140px;
        padding: 5px 0;
    }

    .table > tbody > tr > td, .table > tbody > tr > th, .table > tfoot > tr > td, .table > tfoot > tr > th, .table > thead > tr > td, .table > thead > tr > th {
        padding: 6px 2px;
    }

    input[type=checkbox] {
        width: 100% !important;
        height: 17px !important;
    }
</style>


<script type="text/javascript">
    $(document).ready(function () {
        $('.my-select').select2({
            placeholder: "--Select--",
            allowClear: true,
            width: '100%'
        });

    });
    LayoutApp.controller('MyController', function ($scope, $http, $timeout) {
        $('#overlay').fadeIn().delay(300).fadeOut();

        $scope.MergeBLRec1 = [{ Size: "", CntrNo: "", CommodityTypes: "", BCNID: "", Selected: "" }];
       // BL DROPDOWN1
        $scope.BLNumberlist1 = null;
        $scope.fillBlNumber1 = function () {
            $http({
                method: 'POST',
                url: '/api/BookingApi/BookingBLList/',
                data: {}
            }).then(function (result) {
                $timeout(function () {
                    $scope.BLNumberlist1 = result.data;

                });
            }, function (result) {

                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected. Please reconnect and try again.");
                }
            });

        };
        $scope.fillBlNumber1();

        //onchange BL DROPDOWN1 grid1
        $("#ddlBLNo1").change(function () {
            $scope.MergeBLRec1 = null;
            var Datavalue = {};

            Datavalue.BLID = $("#ddlBLNo1").val();
            $http({
                method: 'POST',
                url: '/api/BookingApi/ViewMergeBLGrid1/',
                data: JSON.stringify(Datavalue),
               
            }).then(function (result) {
                $timeout(function () {
                    $scope.MergeBLRec1 = result.data;
                });
            }, function (result) {
                if (result.status == 500) {
                    ShowPopup("Sorry no internet connectivity Application Error.");
                }
                else if (result.status == 404) {
                    ShowPopup("Sorry no internet connectivity page not found.");
                }
                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            }).catch(function (result) {
                ShowPopup('There has been an error: ' + result);
            });
            
        });

        //$scope.BindGrid1AfterMove= function (BLID) {
        //    alert(BLID);
        //    $scope.MergeBLRec1 = null;
        //    var Datavalue = {};

        //    Datavalue.BLID = BLID;
        //    $http({
        //        method: 'POST',
        //        url: '/api/BookingApi/ViewMergeBLGrid1/',
        //        data: JSON.stringify(Datavalue),

        //    }).then(function (result) {
        //        $timeout(function () {
        //            $scope.MergeBLRec1 = result.data;
        //        });
        //    }, function (result) {
        //        if (result.status == 500) {
        //            ShowPopup("Sorry no internet connectivity Application Error.");
        //        }
        //        else if (result.status == 404) {
        //            ShowPopup("Sorry no internet connectivity page not found.");
        //        }
        //        if (result.status > 0) {
        //            var errorMsg = result.status + ': ' + result.data;
        //            ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
        //        }
        //    }).catch(function (result) {
        //        ShowPopup('There has been an error: ' + result);
        //    });
        //};


        // BL DROPDOWN2
        $scope.BLNumberlist2 = null;
        $scope.fillBlNumber2 = function () {
            $http({
                method: 'POST',
                url: '/api/BookingApi/BookingBLList/',
                data: {}
            }).then(function (result) {
                $timeout(function () {
                    $scope.BLNumberlist2 = result.data;

                });
            }, function (result) {

                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected. Please reconnect and try again.");
                }
            });

        };
        $scope.fillBlNumber2();

        //onchange BL DROPDOWN2 grid2
        $("#ddlBLNo2").change(function () {
            $scope.MergeBLRec2 = null;
            var Datavalue = {};

            Datavalue.BLID = $("#ddlBLNo2").val();
            $http({
                method: 'POST',
                url: '/api/BookingApi/ViewMergeBLGrid1/',
                data: JSON.stringify(Datavalue),

            }).then(function (result) {
                $timeout(function () {
                    $scope.MergeBLRec2 = result.data;
                });
            }, function (result) {
                if (result.status == 500) {
                    ShowPopup("Sorry no internet connectivity Application Error.");
                }
                else if (result.status == 404) {
                    ShowPopup("Sorry no internet connectivity page not found.");
                }
                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            }).catch(function (result) {
                ShowPopup('There has been an error: ' + result);
            });
        });

        //$scope.BindGrid2AfterMove = function (BLID) {
        //    alert(BLID);
        //    $scope.MergeBLRec2 = null;
        //    var Datavalue = {};

        //    Datavalue.BLID = BLID;
        //    $http({
        //        method: 'POST',
        //        url: '/api/BookingApi/ViewMergeBLGrid1/',
        //        data: JSON.stringify(Datavalue),

        //    }).then(function (result) {
        //        $timeout(function () {
        //            $scope.MergeBLRec2 = result.data;
        //        });
        //    }, function (result) {
        //        if (result.status == 500) {
        //            ShowPopup("Sorry no internet connectivity Application Error.");
        //        }
        //        else if (result.status == 404) {
        //            ShowPopup("Sorry no internet connectivity page not found.");
        //        }
        //        if (result.status > 0) {
        //            var errorMsg = result.status + ': ' + result.data;
        //            ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
        //        }
        //    }).catch(function (result) {
        //        ShowPopup('There has been an error: ' + result);
        //    });
        //};
       
    


     


        $scope.MoveBL1ToBL2 = function () {
          
            var val;

            angular.forEach($scope.MergeBLRec1, function (value, key) {
                if ($scope.MergeBLRec1[key].Selected == true) {
                    val = {
                        'Size': $scope.MergeBLRec1[key].Size,
                        'CntrNo': $scope.MergeBLRec1[key].CntrNo,
                        'CommodityTypes': $scope.MergeBLRec1[key].CommodityTypes,
                        //'BCNID': $scope.MergeBLRec1[key].BCNID,
                        'Selected': $scope.MergeBLRec1[key].Selected
                    };
                    if ($scope.MergeBLRec2.length > 0) {
                        angular.forEach($scope.MergeBLRec2, function (value, key1) {
                            if ($scope.MergeBLRec2[key1].CntrNo == $scope.MergeBLRec1[key].CntrNo) {
                                $scope.MergeBLRec2[$scope.MergeBLRec1[key].CntrNo] = val;
                                $scope.MergeBLRec1.splice(0, key);
                            }
                            else {
                                $scope.MergeBLRec2.push(val);
                                $scope.MergeBLRec1.splice(key);
                            }
                        });
                    }
                    else {
                        $scope.MergeBLRec2.push(val);
                        $scope.MergeBLRec1.splice(key, 1);
                    }
                }
            });
           
        }

        $scope.MoveBL2ToBL1 = function (index) {
            var val;
           
            angular.forEach($scope.MergeBLRec2, function (value, key)
            {
                if ($scope.MergeBLRec2[key].Selected == true) {
                    val = {
                        'Size': $scope.MergeBLRec2[key].Size,
                        'CntrNo': $scope.MergeBLRec2[key].CntrNo,
                        'CommodityTypes': $scope.MergeBLRec2[key].CommodityTypes,
                        //'BCNID': $scope.MergeBLRec2[key].BCNID,
                        'Selected': $scope.MergeBLRec2[key].Selected
                    };
                    if ($scope.MergeBLRec1.length > 0)
                    {
                        angular.forEach($scope.MergeBLRec1, function (value, key1)
                        {
                            
                            if ($scope.MergeBLRec1[key1].CntrNo == $scope.MergeBLRec2[key].CntrNo) {
                             
                                $scope.MergeBLRec1[$scope.MergeBLRec2[key].CntrNo] = val;
                                $scope.MergeBLRec2.splice(0, key);
                            }
                            else {
                               
                                $scope.MergeBLRec1.push(val);
                                $scope.MergeBLRec2.splice(key);
                            }
                        });
                    }
                    else
                    {
                      
                        $scope.MergeBLRec1.push(val);
                        $scope.MergeBLRec2.splice(key, 1);
                    }
                }
            });
          

        }

        $scope.MergeBLUpdate = function () {
            var validation = "";

            var ddlBLNo1 = $('#ddlBLNo1').val();
            if (ddlBLNo1 == "?") {
                validation += "<span style='color:red;'>*</span> <span>Please Select Booking/BL-1 </span></br>"
            }

            var ddlBLNo2 = $('#ddlBLNo2').val();
            if (ddlBLNo2 == "?") {
                validation += "<span style='color:red;'>*</span> <span>Please Select Booking/BL-2 </span></br>"
            }
            if (validation != "") {

                ShowPopup(validation);
                return false;
            }


            var Datavalue = {};
            Datavalue.BLID1 = $('#ddlBLNo1').val();
            Datavalue.BLID2 = $('#ddlBLNo2').val();
            var Items1 = [];
            var Items2 = [];
            console.log($scope.MergeBLRec1);
            console.log($scope.MergeBLRec2);

            for (var i = 0; i < $scope.MergeBLRec1.length; i++)
            {
                var CntrId1 = $scope.MergeBLRec1[i].BCNID;
                Items1.push("Insert:" + CntrId1);
            }
            for (var i = 0; i < $scope.MergeBLRec2.length; i++) {
                var CntrId2 = $scope.MergeBLRec2[i].BCNID;
                Items2.push("Insert:" + CntrId2);
            }

            Datavalue.Items1 = Items1.toString();
            Datavalue.Items2 = Items2.toString();
            $http({
                method: 'POST',
                url: '/api/BookingApi/ViewMergeBLUpdate/',
                data: JSON.stringify(Datavalue),

            }).then(function (result)
            {
                $timeout(function ()
                {
                    
                });
            }, function (result) {
                if (result.status == 500) {
                    ShowPopup("Sorry no internet connectivity Application Error.");
                }
                else if (result.status == 404) {
                    ShowPopup("Sorry no internet connectivity page not found.");
                }
                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            }).catch(function (result) {
                ShowPopup('There has been an error: ' + result);
            });


            ShowPopup("BL Merged SuccessFully");
        }
        function ShowPopup(message) {
            swal(message);
        };
    });
</script>
<div ng-app="MyApp" ng-controller="MyController">
    <div id="overlay" style="display:none;">
        <div class="loadcontainer">
            <div class="spinner"></div>
            <p>Loading Please Wait</p>
        </div>
    </div>
    <div class="container-fluid">
        @*<div class="row">
            <div class="col-md-12 backbtn" style="text-align:right;">
                <a href="#" class="btn btn-fill btn-success btnback btn-round btntop">
                    <i class="material-icons">reply</i> Back
                </a>


            </div>
        </div>*@
        <div class="row">
            <div class="col-md-12">
                <div class="card ">
                    <div class="card-header card-header-primary card-header-text">
                        <div class="card-text">
                            <h4 class="card-title">Merge BL</h4>
                        </div>
                    </div>
                    <div class="card-body ">
                        <div class="row">
                            <div class="col-md-5">
                                <div class="row">
                                    <div class="col-md-12">
                                        <div class="form-group">
                                            <div class="col-md-12 padlr0">
                                                <label>BL-1</label>
                                                <select class="form-control my-select" ng-model="ddlBLNo1" id="ddlBLNo1" ng-options="item.BLNumber for item in BLNumberlist1 track by item.ID" required>
                                                </select>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                        

                                <div class="row">
                                    <div class="col-md-12">
                                        <div class="material-datatables">

                                            <table class="table table-striped table-no-bordered table-hover" cellspacing="0" width="100%" style="width:100%">
                                                <thead>
                                                    <tr>
                                                        <th>S.No.</th>
                                                        <th>Size Type</th>
                                                        <th>Container Number</th>
                                                        <th>Commodity</th>
                                                        <th>Flag</th>

                                                    </tr>
                                                </thead>

                                                <tbody>
                                                    <tr ng-repeat="i in MergeBLRec1">
                                                        <td>{{$index+1}}</td>
                                                        <td>{{i.Size}}</td>
                                                        <td>{{i.CntrNo}}</td>
                                                        <td>{{i.CommodityTypes}}</td>
                                                        <td> <input type="checkbox" id="chkflag1" value="{{i.BCNID}}" ng-model="i.Selected" /></td>

                                                    </tr>

                                                </tbody>
                                            </table>

                                        </div>
                                    </div>

                                </div>
                            </div>
                            <div class="col-md-2">
                                <div class="col-md-12 padl0" style="margin-top:150px;">
                                    <div class="row">
                                        <div class="col-md-12">
                                            <button type="submit" class="btn btn-fill btn-success btnmove1" ng-click="MoveBL2ToBL1();">Move to <br />Booking - 1</button>
                                        </div>
                                        <div class="col-md-12">
                                            <button type="submit" class="btn btn-fill btn-success btnmove2" ng-click="MoveBL1ToBL2();">Move to <br />Booking - 2</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-5">
                                <div class="row">
                                    <div class="col-md-12">
                                        <div class="form-group">
                                            <div class="col-md-12 padlr0">
                                                <label>BL-2</label>
                                                <select class="form-control my-select" ng-model="ddlBLNo2" id="ddlBLNo2" ng-options="item.BLNumber for item in BLNumberlist2 track by item.ID" required>
                                                </select>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-md-12">
                                        <div class="material-datatables">

                                            <table class="table table-striped table-no-bordered table-hover" cellspacing="0" width="100%" style="width:100%">
                                                <thead>
                                                    <tr>
                                                        <th>S.No.</th>
                                                        <th>Size Type</th>
                                                        <th>Container Number</th>
                                                        <th>Commodity</th>
                                                        <th>Flag</th>

                                                    </tr>
                                                </thead>

                                                <tbody>
                                                    <tr ng-repeat="k in MergeBLRec2 | filter : paginate | orderBy:'-Statusv'">
                                                        <td>{{$index+1}}</td>
                                                        <td>{{k.Size}}</td>
                                                        <td>{{k.CntrNo}}</td>
                                                        <td>{{k.CommodityTypes}}</td>
                                                        <td> <input type="checkbox" id="chkflag2" value="{{k.BCNID}}" ng-model="k.Selected" /></td>

                                                    </tr>
                                                </tbody>
                                            </table>

                                        </div>
                                    </div>

                                </div>
                            </div>
                        </div>



                    </div>
                    <div class="card-footer ">
                        <div class="col-md-12" style="text-align:right;">
                            <button type="submit" class="btn btn-fill btn-danger">Cancel</button>
                            <button type="submit" ng-click="MergeBLUpdate();" class="btn btn-fill btn-success">Merge</button>

                        </div>
                    </div>
                </div>
            </div>

        </div>

    </div>
</div>
