@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/css/select2.min.css" rel="stylesheet">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.3/js/select2.full.min.js"></script>
<style>
    .table td, .table th {
        border: 1px solid #fff;
        padding: 4px;
    }
    .table td, .table td {
        border: 1px solid #ccc;
        padding: 4px;
        font-weight:800;
    }
    .form-control {
        width: 180px;
        height: 24px;
    }
    .table > thead > tr > th, .table > tbody > tr > th, .table > tfoot > tr > th {
        background-color: #12759a !important;
        color: #fff;
    }
    .material-datatables .table thead {
        background-color: #12759a !important;
        color: #fff;
    }
</style>
<script type="text/javascript">

    LayoutApp.controller('MyController', function ($scope, $http, $timeout) {
        $(document).ready(function () {
            $('.my-select').select2({
                placeholder: "--Select--", allowClear: true, width: '100%'
            });
        });


        $scope.GeoLocationList = null;
        $scope.fillGeoLocations = function () {
            $http({
                method: 'POST',
                url: '/api/Home/GeoLocationsMaster/',
                data: {}
            }).then(function (result) {
                $timeout(function () {
                    $scope.GeoLocationList = result.data;

                });
            }, function (result) {
                if (result.status == 500) {
                    ShowPopup("Sorry no internet connectivity Application Error.");
                }
                else if (result.status == 404) {
                    ShowPopup("Sorry no internet connectivity page not found.");
                }
                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            }).catch(function (result) {
                ShowPopup('There has been an error: ' + result);
            });
        };
        $scope.fillGeoLocations();

       $scope.GridViewData = null;
        $scope.fillDCMRLOCReportview = function () {
            var BussData = {};
         
            BussData.GeoLoc = $('#ddlGeoLoc').val();

            $http({
                method: 'POST',
                url: '/api/EQCReportApi/DCMRLocationWiseReportView/',
                data: JSON.stringify(BussData),
            }).then(function (result) {
                $timeout(function () {
                    $scope.GridViewData = result.data;
                    $scope.totalItems = $scope.GridViewData.length;
                    $scope.currentPage = 1;
                    $scope.numPerPage = 8;
                    $scope.paginate = function (value) {
                        var begin, end, index;
                        begin = ($scope.currentPage - 1) * $scope.numPerPage;
                        end = begin + $scope.numPerPage;
                        index = $scope.GridViewData.indexOf(value);
                        return (begin <= index && index < end);
                    };


                });
            }, function (result) {
                if (result.status == 500) {
                    ShowPopup("Sorry no internet connectivity Application Error.");
                }
                else if (result.status == 404) {
                    ShowPopup("Sorry no internet connectivity page not found.");
                }
                if (result.status > 0) {
                    var errorMsg = result.status + ': ' + result.data;
                    ShowPopup("Sorry no internet connectivity detected.Please reconnect and try  again.");
                }
            }).catch(function (result) {
                ShowPopup('There has been an error: ' + result);
            });
        };
        //$scope.fillDCMRLOCReportview();
      
        $scope.btnSearch = function () {
            var validation = "";
            var ddlGeoLoc = $('#ddlGeoLoc').val();
            if (ddlGeoLoc == "?") {

                validation += "<span style='color:red;'>*</span> <span>Please Select Location</span></br>"
            }
            if (validation != "") {

                ShowPopup(validation);
                return false;
            }
            $scope.fillDCMRLOCReportview();
        }
        $scope.ClearRecords = function () {
            $("#ddlGeoLoc").val(null).trigger("change");
            $scope.fillDCMRLOCReportview();
        }
        $scope.btnPrint = function () {
            var validation = "";
            var GeoLoc = $('#ddlGeoLoc').val();
            var User = localStorage.getItem("UserName");
            if (GeoLoc == "?") {

                validation += "<span style='color:red;'>*</span> <span>Please Select Location</span></br>"
            }
            if (validation != "") {

                ShowPopup(validation);
                return false;
            }
            window.location.href = '@Url.Action("DCMRLocWiseExcelReport", "EQCReport")?GeoLoc=' + GeoLoc + "&User=" + User;


        }

    });
    function ShowPopup(message) {
        swal(message);
    };
</script>
<style>
    legend {
        background-color: #0585b5;
        color: #fff;
        font-size: 14px;
        padding: 4px 5px;
        font-weight: bold;
    }

    .inventry .form-control.sundry {
        width: 172px !important;
    }

    .table > thead > tr > th, .table > tbody > tr > th, .table > tfoot > tr > th {
        background-color: #12759a !important;
        color: #fff;
    }
    .table > tbody > tr > td, .table > tfoot > tr > td {
        background-color: #4a55e954;
    }
    .table > thead > tr > th {
        text-align: center;
        background-color: #6e74db;
        color: #fff;
    }
    .nav-tabs {
        width: 300px !important;
        margin: 0 auto !important;
    }
    .form-control {
        width: 180px;
        height: 24px;
    }

    .card .table tr td {
        white-space: nowrap;
    }

    .exceldownload .fa-file-excel-o {
        color: #4caf50;
        font-weight: 400;
        font-size: 20px;
    }
</style>
@*<link href="~/assets/css/common.css" rel="stylesheet" />
<link href="~/assets/css/InvoiceStyle.css" rel="stylesheet" />*@
<div ng-app="MyApp" ng-controller="MyController">
    <div class="row">
        <div class="col-md-12 backbtn" style="text-align:right;">
            <a href="/AllReports/InventoryReportDashboard" type="submit" class="btn btn-fill btnback btn-success btn-round btntop">
                <i class="material-icons">reply</i> Back
            </a>

        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                


                <div class="card-body">
                    <div class="row" style="border: 1px solid #ccc;">
                        <div class="col-md-2" style="text-align:center;">
                            <img src="~/assets/img/misrepimg.jpg" style="width:100px;" />
                        </div>
                        <div class="col-md-8" style="text-align:center;">
                            <h4 style="font-size:24px; font-weight:bold; color:#000!important; margin-top:39px;">DCMR Locationwise Report</h4>
                        </div>
                        <div class="col-md-2" style="text-align:center;">
                            <img src="~/assets/img/misrepimg.jpg" style="width:100px;" />
                        </div>
                    </div>

                    <div class="row" style="border: 1px solid #ccc; margin-top:15px;">

                        <div class="col-md-12">
                            <div class="row">

                                <div class="col-md-6">
                                    <div class="form-group">
                                        <div class="row">
                                            <div class="col-md-4 lbl2"><label class="lable-fond">Location</label></div>
                                            <div class="col-md-1">:</div>
                                            <div class="col-md-5">
                                                <select id="ddlGeoLoc" class="form-control my-select" ng-model="ddlGeoLoc" options="{ allowClear: true }" ng-options="item.GeoLocation for item in GeoLocationList track by item.ID"></select>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="col-md-6" style="text-align:center;">
                                    <div class="form-group">
                                        <div class="row">
                                            <div class="col-md-12">
                                                <button type="submit" class="btn btn-fill btn-info" ng-model="btnSearch" ng-click="btnSearch();" style="padding: 6px 18px; margin-top: -1px;width:46%; ">
                                                    <i class="material-icons"></i> Search
                                                </button>
                                                <button type="submit" class="btn btn-fill btn-default" ng-model="ClearRecords" ng-click="ClearRecords();" style="padding: 6px 18px; margin-top: -1px; width: 46%;">
                                                    <i class="material-icons">close</i> Clear
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-12" style="text-align:right;">
                            <div class="row">
                                <div class="col-md-12 exceldownload" style="text-align:right;">
                                    <a ng-click="btnPrint();">Download <i class="fa fa-file-excel-o"></i></a>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="row" style="border: 1px solid #ccc; margin-top:15px;">
                                <div class="col-md-12 padlr0" style="padding:7px;">

                                    <div class="material-datatables">

                                        <table class="table table-striped table-no-bordered table-hover" cellspacing="0" width="100%" style="width:100%">
                                            <thead>
                                                <tr>
                                                    <td style="text-align:center;">CURRENT STATUS</td>


                                                    <td style="text-align:center;" colspan="4">EQUIPMENT</td>
                                                </tr>
                                                <tr>
                                                    <td>&nbsp;</td>
                                                    <td>20'GP'</td>
                                                    <td>20'HC'</td>
                                                    <td>40'GB'</td>
                                                    <td>40'HC'</td>
                                                </tr>

                                            </thead>
                                            <tbody>
                                                <tr ng-repeat="gRow in GridViewData | filter : paginate | orderBy:'-Statusv'">
                                                    <td>{{gRow.Description}}</td>
                                                    <td>{{gRow.DCMRLoc20GP}}</td>
                                                    <td>{{gRow.DCMRLoc20HC}}</td>
                                                    <td>{{gRow.DCMRLoc40GP}}</td>
                                                    <td>{{gRow.DCMRLoc40HC}}</td>
                                                </tr>
                                            </tbody>
                                        </table>

                                    </div>
                                </div>
                            </div>
                        </div>

                    </div>



                    </div>


                    </div>



                </div>




            </div>
        </div>
    

<script>
    $(document).ready(function () {
        // initialise Datetimepicker and Sliders
        $('.datepicker').datetimepicker({
            format: 'DD/MM/YYYY',

        });
    });
</script>
